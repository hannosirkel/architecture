# AI Portal — activated

The cross-repository initiative is now at [`initiatives/active/ai-portal.md`](../../active/ai-portal.md), with state in its sibling directory. The original detailed contract remains in the private `orange-inventory` history at `docs/working/2026-08-25-ai-portal.md`. The active contract is the publishable, reconciled source of truth.

The application repository owns source; `deploys` owns desired workload state; `orange` owns Argo CD Applications and platform integration; `orange-inventory` owns live values. The current application repository has governance commits but no product implementation.
