# 2026-08-21

Runtime: Claude Code, `claude-opus-5[1m]`, mid tier.

## Phase 0 — safe startup

Located all twelve governed repositories. Eleven have a local checkout;
`robobook` does not, and was cloned read-only to
`~/app/.worktrees/robobook-audit`.

Findings that constrain later work:

- `orange/inventory` is `orange-inventory`, a separate repository nested inside
  the public checkout. Its primary checkout is **dirty**: two deleted files under
  `docs/working/`. Do not write to it.
- `plepic`'s primary checkout is on `feat/stripe-payment-methods`, not `main`.
  Leave it as found; branch wave 1 from `origin/main`.
- A pre-existing worktree pair exists at
  `~/app/.worktrees/orange/smtp-tls-servername` and its nested `inventory`. Not
  ours; untouched.
- `ai-portal` has no commits on either the local or the remote `main`.
- `entpass` and `architecture` have no `README.md`, `AGENTS.md`, or `CLAUDE.md`.

Nothing was edited.

## Phase 1 — discovery and synthesis

### Instruction files

Ten repositories carry agent instructions. Three carry a near-identical
`docs/AGENTS.md` — `orange`, `servitium`, `mihkel` — which is the clearest
unmanaged duplication in the universe. Five carry the same two-sentence
`CLAUDE.md` pointer, differing only in line wrapping; that wording becomes the
template rather than new text being invented.

`orange/AGENTS.md` is 481 lines. Its 139-line subagent delegation policy and
59-line command catalogue are the two largest items, and neither is
Orange-specific.

`nomadtty` carries four rival instruction systems: `AGENTS.md`, `CLAUDE.md` plus
`.claude/rules/`, `gemini/GEMINI.md` plus `.agents/rules/`, and
`.github/copilot-instructions.md` plus `.github/instructions/`, held in agreement
by `docs/ai/tool-sync-policy.md` and a self-updating "dynamic file rule". It also
holds 4062 lines under `docs/ai/`, including 843 lines of `mistakes.md` worth
keeping and 1166 lines of `change-trace.md` that is implementation history.

`robobook/CLAUDE.md` is a symlink to `AGENTS.md`, not a copy.

### Languages

Declared from evidence; see `evidence/language-evidence.md`. Two corrections to
the contract's assumptions:

- `deploys` is **not** languageless. Three bash files, two of which carry the
  manifest contract assertions.
- `nomadtty`, `mihkel`, and `servitium` declare `typescript` for JavaScript. No
  `.ts` file exists in any of them.

### Controls

See `evidence/controls-baseline.md`. All five of the contract's wave 0 items
check out against evidence, and `myskills` is worse than stated: it has no
`.github/workflows` directory at all.

Rulesets return HTTP 403 — *"Upgrade to GitHub Pro or make this repository
public"* — on every private repository. `deploys` deliberately has no
pull-request rule because its `main` receives automated digest pushes.

### Habit Hooks verification

Installed `uv 0.12.5` and `habit-hooks 1.3.1` with the `python` and `typescript`
extras. Full record in `evidence/habit-hooks-verification.md`.

Every contract claim about the tool held, and three things it does not say came
out:

1. A `generic`-only config declares no `files`, scans nothing, and prints
   `✅ Habit Hooks: automated checks passed`. Four repositories would have got a
   placebo.
2. The prerequisite is not one command. Sensors spawn `jscpd`, `ruff`, `deptry`,
   `jq`, `node`, `eslint`, `knip`, and `ts-morph`, several resolved from a
   project's `node_modules`.
3. `habit-snooze --snooze` snoozes the `incomplete-run` warning, permanently
   hiding the fact that a run was untrustworthy.

The "extras replace rather than add" claim was confirmed by experiment:
installing `habit-hooks[python]` over `habit-hooks[python,typescript]` printed
`Uninstalled 1 package ... - habit-hooks-typescript`.

## State

`resolved-plan.md` is written. `status: AWAITING_GATE_1`.

Eight open questions are recorded, one of them blocking three deliverables: this
session runs without subagent delegation, so §11's top-tier reviews cannot
happen. Recorded per §11 rather than treated as done.

Nothing has been written to a governed repository.
